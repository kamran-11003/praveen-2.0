flask>=3.0
flask-cors>=4.0
flask-socketio>=5.3
python-dotenv>=1.0

# LLM — Ollama runs as a local service (install separately: https://ollama.com)
ollama>=0.4

# RAG — fully local, no API needed
sentence-transformers>=3.0
faiss-cpu>=1.8
numpy>=1.26

# PDF + OCR
pymupdf>=1.24            # fitz
pytesseract>=0.3.10      # requires Tesseract binary on PATH
Pillow>=10.0

# STT — faster-whisper (local GPU inference)
faster-whisper>=1.0

# TTS
edge-tts>=6.1

# Misc
requests>=2.31
